const initialState = {
  sorting: 'Alphabetically (A to Z)',
};

export default function sortReducer(state = initialState, action) {
  switch (action.type) {
    default: {
      return state;
    }
  }
}